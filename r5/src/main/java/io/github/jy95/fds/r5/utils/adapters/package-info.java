/**
 * Adapters converting HL7 FHIR R5 types to the library's internal shapes.
 *
 * <p>Contains adapter classes that transform HL7 FHIR R5 model instances
 * into the common, version-independent types used by the core rendering and
 * translation code. R5 adapters handle R5-specific model differences and
 * expose a normalized view for the shared logic.</p>
 */
package io.github.jy95.fds.r5.utils.adapters;